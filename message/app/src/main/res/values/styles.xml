<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name  = "login_Textstyle">
        <item name="android:textColor">@color/textColor1</item>
        <item name="android:textSize">@dimen/textSize1</item>

    </style>
    <style name="buttonbackgroundColor">
        <item name="android:background">@drawable/bt_slpash_register_selector</item>
        <item name="android:textColor">@color/colorwhile</item>
    </style>

</resources>
